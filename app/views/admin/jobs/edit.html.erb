<div class="col-md-4 col-md-offset-4">
  <h2 class="text-center">Edit a Job(admin)</h2>
  <hr>
  <%= simple_form_for [:admin, @job] do |f| %>
    <%= f.input :title %>
    <%= f.input :description %>
    <%= f.submit "Submit", class: "btn btn-default" %>
  <% end %>
</div>
